
import Button from '@material-ui/core/Button';

const TeacherList = () => {
  return (
    <>
      
      <Button variant="contained" color="primary" style={{marginTop:10 + 'px'}}>
        Показать Еще
      </Button>
    </>
  );
};

export default TeacherList;